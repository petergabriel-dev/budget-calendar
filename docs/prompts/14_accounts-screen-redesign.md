# 14 — Accounts Screen Redesign

Full visual redesign of the Accounts screen to match the Pencil design spec. UI-only — no domain or database changes. Depends on `13_account-description-field.md` being complete (`Account.description` must exist).

Shadow spec: `specs/shadow/Account.shadow.md`
Design file: `specs/DESIGN_SYSTEM.pen`

---

## Tasks

- [x] Update the screen header in `AccountScreen.kt`. Before implementing, read node `CvyNk` from `specs/DESIGN_SYSTEM.pen` using `mcp__pencil__batch_get`. Replace whatever renders the current top of the accounts screen with a `Row(Modifier.fillMaxWidth(), horizontalArrangement = SpaceBetween, verticalAlignment = CenterVertically)` containing: (1) `Text("Accounts", fontFamily = Outfit, fontSize = 40.sp, fontWeight = W800, letterSpacing = (-1).sp, lineHeight = 38.sp, color = textPrimary)`; (2) a 44×44dp `Box(Modifier.size(44.dp).clip(CircleShape).background(colors.bgSurface).clickable(onClick = onAddAccount), contentAlignment = Center)` containing a `Icon(lucide Plus, 20.dp, tint = textPrimary)`. Remove the floating `BcButton("Add Account", variant = PrimaryIconLeading)` from `AccountListScreen`. After implementing, re-read `CvyNk` and verify fidelity. (Use Skill: kmp) (Reference: specs/shadow/Account.shadow.md)

- [x] Replace `NetWorthSummary` in `AccountListScreen`. Before implementing, read node `vYcfF` from `specs/DESIGN_SYSTEM.pen` using `mcp__pencil__batch_get`. Delete the `NetWorthSummary` composable and its wrapping `Surface`. In its place render a `Column(verticalArrangement = spacedBy(6.dp))` containing: (1) `Text("TOTAL NET WORTH", fontFamily = Outfit, fontSize = 13.sp, fontWeight = W600, letterSpacing = 2.sp, color = colors.textSecondary)`; (2) the existing `HeroNetWorth(amount = netWorth)` composable (verify it uses Outfit 72sp W900 letterSpacing −3sp lineHeight 0.85 — fix if it doesn't). After implementing, re-read `vYcfF` and verify fidelity. (Use Skill: kmp) (Reference: specs/shadow/Account.shadow.md)

- [x] Implement `SpendingPoolCard` at `features/accounts/presentation/components/SpendingPoolCard.kt`. Before implementing, read node `XOueE` from `specs/DESIGN_SYSTEM.pen` using `mcp__pencil__batch_get`. Signature: `fun SpendingPoolCard(account: Account, balance: Long, onClick: () -> Unit, modifier: Modifier = Modifier)`. Layout: `Column(modifier.fillMaxWidth().clip(RoundedCornerShape(20.dp)).background(Color.Black).clickable(onClick).padding(16.dp), verticalArrangement = spacedBy(16.dp))`. Top: `Box(Modifier.clip(RoundedCornerShape(16.dp)).background(Color.White).padding(horizontal = 12.dp, vertical = 6.dp))` containing `Text(account.type.name, fontFamily = Outfit, 11.sp, W700, color = Color.Black)`. Bottom `Column(verticalArrangement = spacedBy(6.dp))`: `Text(balance.formatCurrency(), fontFamily = Outfit, 24.sp, W800, color = Color.White, lineHeight = (24 * 1.1).sp)`; `Text(account.description ?: "", fontFamily = Inter, 12.sp, W400, color = Color(0xFFA1A1AA), maxLines = 1, overflow = Ellipsis)`. After implementing, re-read `XOueE` and verify fidelity. (Use Skill: kmp) (Reference: specs/shadow/Account.shadow.md)

- [x] Implement `WalledGardenCard` at `features/accounts/presentation/components/WalledGardenCard.kt`. Before implementing, read node `iDE7I` from `specs/DESIGN_SYSTEM.pen` using `mcp__pencil__batch_get`. Signature: `fun WalledGardenCard(account: Account, balance: Long, isLiability: Boolean = false, onClick: () -> Unit, modifier: Modifier = Modifier)`. Layout: `Column(modifier.clip(RoundedCornerShape(20.dp)).background(Color(0xFFF4F4F5)).clickable(onClick).padding(16.dp), verticalArrangement = spacedBy(16.dp))`. Badge: when `isLiability = false`, pill is `fill=Color.Black, text color=Color.White`; when `isLiability = true`, pill is `fill=colors.bgSurface, border = BorderStroke(1.5.dp, colors.borderStrong), text color=colors.textSecondary`. Badge pill: `clip(RoundedCornerShape(16.dp))`, `padding(horizontal=12.dp, vertical=6.dp)`, text `Outfit 11sp W700`. Content `Column(spacedBy(6.dp))`: `Text(balance.formatCurrency(), Outfit 24sp W800, color = if isLiability Color.Black else Color.Black, lineHeight 1.1)`; `Text(account.description ?: account.name, Inter 12sp W400, color = Color(0xFF71717A), maxLines=1, overflow=Ellipsis)`. After implementing, re-read `iDE7I` and verify fidelity. (Use Skill: kmp) (Reference: specs/shadow/Account.shadow.md)

- [x] Refactor `AccountListScreen` to group accounts into three sections with section subtotals and responsive layout for Savings & Investments. Before implementing, read nodes `O3ux1`, `2iy1W`, `DJEps`, `1pfxS` from `specs/DESIGN_SYSTEM.pen` using `mcp__pencil__batch_get`. Changes: (1) Compute groups inside the composable: `val spendingPool = accounts.filter { it.isInSpendingPool }` (non-CC accounts already excluded by caller), `val wallGarden = accounts.filter { !it.isInSpendingPool }`, credit cards already a separate param. (2) Create a private `SectionHeader(title: String, total: Long)` composable matching `O3ux1`: `Row(Modifier.fillMaxWidth(), horizontalArrangement = SpaceBetween, verticalAlignment = Bottom)` with `Text(title, Outfit 24sp W800 letterSpacing -0.5sp)` and `Text(total.formatCurrency(), Inter 14sp W500 textSecondary)`. (3) Spending Pool section: `SectionHeader("Spending Pool", spendingPoolTotal)` + stacked `SpendingPoolCard` items (`fillMaxWidth`). (4) Savings & Investments section: `SectionHeader("Savings & Investments", wallGardenTotal)` + `BoxWithConstraints` — if `maxWidth < 600.dp` render a single-column `Column`; if `≥ 600.dp` render a two-column `Row` wrapping grid with 16dp gap. Each card is a `WalledGardenCard`. (5) Credit Cards section: `SectionHeader("Credit Cards", creditCardsTotal)` + single-column `WalledGardenCard(isLiability = true)` items. (6) Remove old `AccountCard` usages and `BcSectionHeader("Accounts")`. After implementing, re-read the section nodes and diff for visual fidelity. (Use Skill: kmp) (Reference: specs/shadow/Account.shadow.md)
